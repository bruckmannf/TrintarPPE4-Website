{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <div class="text-center">
                <h1>MODIFICATION DES UTILISATEURS</h1>
            </div>
        </div>
    </div>
    <div class="container mt-4">
        {{ include ('admin/utilisateur/_form.html.twig', {form: form, button: 'Modifier un utilisateur'}) }}
    </div>

{% endblock %}